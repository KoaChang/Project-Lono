/* Reset default margins and paddings */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Full-screen body with flexbox centering */
body {
    width: 100vw;
    height: 100vh;
    background-color: #121212; /* Dark background for contrast */
    display: flex;
    justify-content: center;
    align-items: center;
    font-family: Arial, sans-serif;
}

/* Parent container to ensure responsiveness */
.chatbot-container {
    width: 90vw;
    height: 90vh;
    display: flex;
    justify-content: center;
    align-items: center;
}

/* Container for the entire chatbot */
.asklono-chatbot {
    width: 100%;
    height: 100%;
    max-width: 1200px; /* Maximum width for larger screens */
    max-height: 800px;  /* Maximum height */
    background-color: #000;
    color: #fff;
    border-radius: 15px;
    display: flex;
    flex-direction: column;
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
    overflow: hidden;
}

/* Header section */
.asklono-header {
    padding: 20px;
    background-color: #1a1a1a;
    text-align: center;
    border-bottom: 2px solid #333;
}

.asklono-header h2 {
    margin: 0;
    font-size: 24px; /* Reduced font size */
    color: #fff;
}

/* Message container with scroll */
.asklono-message-container {
    flex: 1;
    padding: 20px;
    overflow-y: auto;
    background-color: #000;
    font-size: 14px; /* Reduced font size */
}

/* Individual message styling */
.asklono-message {
    margin-bottom: 15px;
    position: relative;
    padding-right: 25px;
    word-wrap: break-word;
}

.asklono-user-message {
    color: #fff;
}

.asklono-assistant-message {
    color: #fff;
    background-color: #1a1a1a;
    padding: 10px;
    border-radius: 5px;
}

/* Delete icon styling */
.asklono-delete-icon {
    position: absolute;
    top: 0;
    right: 0;
    cursor: pointer;
    color: #ff4d4d;
    font-weight: bold;
    font-size: 16px;
}

/* Input area styling */
.asklono-input-wrapper {
    display: flex;
    padding: 10px;
    background-color: #1a1a1a;
    border-top: 2px solid #333;
}

.asklono-user-input {
    flex: 1;
    resize: none;
    padding: 10px;
    border: none;
    border-radius: 5px;
    font-size: 14px; /* Reduced font size */
    background-color: #333;
    color: #fff;
    max-height: 100px; /* Maximum height for the input box */
    overflow-y: auto; /* Enable scrollbar when max height is reached */
    line-height: 1.4;
}

.asklono-user-input:focus {
    outline: none;
    background-color: #444;
}

.asklono-submit-btn {
    margin-left: 10px;
    padding: 10px 20px;
    background-color: #74a84a;
    color: #fff;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 14px; /* Reduced font size */
    transition: background-color 0.3s;
}

.asklono-submit-btn:disabled {
    background-color: #555;
    cursor: not-allowed;
}

.asklono-submit-btn:hover:not(:disabled) {
    background-color: #5e913e;
}

/* Scrollbar styling for message container */
.asklono-message-container::-webkit-scrollbar {
    width: 8px;
}

.asklono-message-container::-webkit-scrollbar-track {
    background: #1a1a1a;
}

.asklono-message-container::-webkit-scrollbar-thumb {
    background-color: #555;
    border-radius: 4px;
}

/* Scrollbar styling for input box */
.asklono-user-input::-webkit-scrollbar {
    width: 6px;
}

.asklono-user-input::-webkit-scrollbar-track {
    background: #333;
}

.asklono-user-input::-webkit-scrollbar-thumb {
    background-color: #555;
    border-radius: 3px;
}
